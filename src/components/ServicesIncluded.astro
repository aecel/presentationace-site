---
import "../styles/included_dark.css";

const SECTION_ID = "included";

const TITLE = "Every engagement includes";
const INTRO = "No surprises. These are standard in every project and retainer.";

type IncludedItem = {
  icon: string;
  iconAlt?: string;
  title: string;
  description: string;
};

const items: IncludedItem[] = [
  {
    icon: "/icons/confidential.svg",
    iconAlt: "Confidentiality",
    title: "Confidentiality by default (NDA-friendly)",
    description:
      "I treat all decks, metrics, and drafts as confidential and can sign an NDA if required.",
  },
  {
    icon: "/icons/collaboration.svg",
    iconAlt: "Collaboration",
    title: "Relationship-driven collaboration",
    description:
      "Clear communication, low-friction process, and I learn your preferences over time so each month gets smoother. I’m adaptable to new tools and workflows and I’m committed to continuously improving my craft.",
  },
  {
    icon: "/icons/feedback.svg",
    iconAlt: "Feedback",
    title: "Feedback-friendly, zero ego",
    description:
      "I’m flexible with your direction and I welcome input without judgment. You’ll get a calm, receptive partner who listens, iterates quickly, and stays focused on what will make the deck stronger.",
  },
  {
    icon: "/icons/quality.svg",
    iconAlt: "Quality",
    title: "Executive-ready design quality",
    description:
      "Clear hierarchy, disciplined layout, consistent spacing, and a polished visual system that reads well in decision rooms.",
  },
  {
    icon: "/icons/vector.svg",
    iconAlt: "Editable",
    title: "Fully editable assets (vector-first)",
    description:
      "Icons are provided in SVG/vector format so colors can be adjusted to match your brand. When I use flat illustrations, they’re built as editable vectors whenever possible, so you can update colors, shapes, and labels later without rebuilding the slide.",
  },
  {
    icon: "/icons/animation.svg",
    iconAlt: "Animation",
    title: "Purposeful animation",
    description:
      "Motion used to guide attention and pacing. I include subtle animation by default because it brings energy and polish, but if you’d rather have zero animation, just let me know.",
  },
  {
    icon: "/icons/versioning.svg",
    iconAlt: "Version control",
    title: "Version control + clean file delivery",
    description:
      "Organized files, sensible naming, and edits that keep decks maintainable for future updates.",
  },
  {
    icon: "/icons/brand.svg",
    iconAlt: "Brand consistency",
    title: "On-brand consistency",
    description:
      "Fonts, colors, spacing, and components stay consistent across the deck.",
  },
];

const FOOTNOTE =
  "If you have accessibility requirements (contrast, font sizing) or brand rules, I’ll build within those constraints.";
---

<section id={SECTION_ID} class="included" aria-labelledby="included-title">
  <div class="shell">
    <header class="included__header">
      <h2 id="included-title" class="included__title">{TITLE}</h2>
      <p class="included__intro">{INTRO}</p>
    </header>

    <div class="included__card" role="list" aria-label="Included items">
      {items.map((item) => (
        <div class="included__item" role="listitem">
          <img
            class="included__icon"
            src={item.icon}
            alt={item.iconAlt ?? ""}
            loading="lazy"
            decoding="async"
          />

          <div class="included__content">
            <h3 class="included__itemTitle">{item.title}</h3>
            <p class="included__itemDesc">{item.description}</p>
          </div>
        </div>
      ))}
    </div>

    {FOOTNOTE && <p class="included__footnote">{FOOTNOTE}</p>}
  </div>
</section>